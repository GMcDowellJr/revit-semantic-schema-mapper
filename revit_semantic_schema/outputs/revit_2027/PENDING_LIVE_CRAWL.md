# This directory is pending a live crawl

`raw_index.json`, `api_pages.json`, `node_type_candidates.json`,
`property_relationship_candidates.json`, `method_relationship_candidates.json`,
`enum_catalogs.json`, `candidate_edges.json`, and `summary.md` are not present here yet.

They will appear here after running:

```
python -m revit_schema_mapper --version 2027
```

from an environment with outbound network access to `https://www.revitapidocs.com/`. The
build environment this scaffold was created in had that access blocked — see
`../../docs/crawl_notes.md` ("Network access limitation encountered while building this") for
details, and "What to check on the first real run" for how to validate the output once it
exists.

A fixture-based smoke test proving the crawl -> parse -> classify -> export pipeline works
end-to-end (against static sample HTML, not a live crawl) lives in
`../_fixture_smoke_test/` and is regenerated by `tests/`-adjacent tooling, not by this
command.

Delete this file once a real run has populated this directory.
